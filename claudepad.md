# Claudepad - ClawedCommand

## Session Summaries
- **2026-02-28T80:00:00Z** — Map Generation Overhaul: Replaced pure Perlin noise map gen with hybrid template + noise system. Added MapTemplate enum (Valley, Crossroads, Fortress/Islands stubs) and MapSize enum (Small 48x48, Medium 64x64, Large 96x96). New 14-step pipeline: zone painting → lane carving → noise detail → symmetry → ramps → fords → base sculpting → tiered resources → camps → connectivity repair. Valley: central river with bridge chokepoints + elevated flanking dirt paths. Crossroads: 3 roads at TechRuins center + forest/rock lane separators. Tiered resources (T0 safe near base, T1 natural expansion, T2 contested, T3 MonkeyMine at center). Auto connectivity repair with emergency dirt paths. Refactored: walk_lane_points helper (eliminated 3x duplication), flood_fill_passable helper (eliminated 3x duplication). Fixed: dead code in emergency path, spawn clamp margin, debug_assert for Rotational90. 8 new tests + 1 integration test. 222 workspace tests pass. Code reviewed twice (correctness + refactor). Pushed to GitHub.
- **2026-02-28T75:00:00Z** — Campaign Mission Scripts: Wrote full narrative content for all 23 missions across 6 story files (story/prologue.md through story/act5_still_waters.md). Format: [STAGE] for camera, "dialogue" for speech, `AI VOICE` for AIs, {GAMEPLAY} for triggers. Includes briefings, in-mission dialogue, debriefs, and all 4 endings (Activation/Severance/Stalemate/Synthesis). Added Flicker subplot (3 parts: Act 1 M3, Act 3 M10, Act 5 M18) for secret Ending D. Act 3 choice (Help Rex → Gemineye 5%+Patches captured vs Refuse → status quo) propagates through Acts 4-5. Fixed geographic continuity (Act 3 ending → LLAMA not Croak). Dual code review: correctness found 6 issues (all fixed), refactor recommended INDEX.md and debrief standardization (deferred). Pushed to GitHub.
- **2026-02-28T70:00:00Z** — Zoom LOD System: Two-tier zoom LOD with hysteresis. ZoomTier resource (Tactical/Strategic) with thresholds 2.0/1.8. StrategicIcon colored-dot children per unit (inverse scale for uniform size). Sprite alpha=0 in Strategic via selection system. Run conditions gate health bars, terrain borders, water, selection pulse, death fade to Tactical only. 2× sprite resolution (draw at 1×, nearest-neighbor upscale). Zoom range narrowed 0.5-3.5 (was 0.3-5.0), default 1.2. Has<T> queries in toggle_lod_visuals. Doubled health bar/selection ring dimensions for halved unit_scale. spawn_strategic_icon helper eliminates duplication. 13 new tests. 224 tests pass. Code reviewed twice (correctness + refactor).
- **2026-02-28T65:00:00Z** — Code Review Bug Fixes (9 bugs): Fixed 9 bugs from dual code review. CRITICAL: AI system ordering (chained after cleanup for determinism), AI used TheBox as builder (now uses idle Pawdler). HIGH: resource deposits never depleted (added depletion + depleted-stop), gathering system didn't filter Dead (added Without<Dead>), AI find_build_position returned same tile (spiral offset from base), AI Attack re-issued AttackMove every eval (added attack_ordered flag), build cmd accepted water terrain (added passability check), victory system hardcoded to 2 players (now uses PlayerResources count). 4 new integration tests (deposit depletion, dead gatherer, build-on-water, 3-player victory). 216 tests pass (45+23+61+19+45+23). Code reviewed twice.
- **2026-02-28T60:00:00Z** — Standard RTS Controls: Filled control gaps. Gutted duplicate Bevy text HUD (kept egui panels). Added building info panel (HP, construction %, production queue, rally point). Cancel production button + queue status in command card. Rally point via right-click (building selected → right-click ground). Rally auto-move (trained units walk to rally via MoveTarget). Rally flag visual (green diamond when building selected). Double-click select all of same UnitKind (300ms window, visible units only). 203 tests pass (45+14+61+19+41+23). Code reviewed twice.
- (older entries moved to oldpad.md)
## Key Findings
- Player wants hybrid control: direct unit micro + AI agent delegation
- AI agent approach: fine-tuned Mistral generates code/MCP tool calls from natural language instructions
- Inference routing: server-side for competitive, local for practice/SP
- Player commands override AI commands for conflict resolution
- Must use fixed-point math from day 1 for deterministic simulation
- **Devstral 2 (123B)** for server-side competitive ($0.40/$2.00 per 1M tokens), **Devstral Small 2 (24B)** for local SP (Q4_K_M ~14GB, fits RTX 4090)
- Both models are dense transformers with native tool use, 256K context
- MCP `inputSchema` maps directly to Mistral `parameters` (same JSON Schema)
- Fine-tuning supports tool-use training: JSONL with messages + tools arrays, 9-char random tool call IDs, stringified JSON args
- Fine-tune via Mistral API (~$5-10/run) or self-hosted with `mistral-finetune` repo (LoRA rank 64)
- Competitive play cost estimate: ~$0.72 per player per game
- Rust client: single `reqwest` HTTP client works for both API and local (OpenAI-compatible endpoint)
- **Voice Command System**: core game mechanic, not convenience. Push-to-talk → speech-to-text → intent classification → triggers player-authored Lua scripts
- **Construct Mode**: in-game LLM-powered scripting environment where players vibecode Lua agent scripts. Can be used mid-mission.
- **Lua scripting**: chosen for WASM sandbox scripts. LLMs generate Lua well, players can hand-edit, classic game scripting language
- **Voice Buff Mechanic**: command-specific temporary buffs on voice-commanded units (attack→damage, retreat→speed, etc.). Intentionally exploitable — "touch all units" meta is acceptable
- **Speech-to-Text tech**: Web Speech API primary (free, low latency, Chromium), Whisper.js fallback (cross-browser, offline, ~40MB model)
- **Intent classification**: tiered — keyword/regex (<50ms) → fuzzy match (~100ms) → Mistral agent for complex strategy (1-3s)
- **Precedents**: EndWar (70-word vocab, hierarchical commands), Radio General (errors as fiction), Warkestra (hybrid voice+mouse)
- **Picovoice Rhino**: audio-to-intent (no transcription step), free tier 3 users, $6K/yr paid. Worth evaluating for production.

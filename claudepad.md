# Claudepad - ClawedCommand

## Session Summaries
- **2026-03-01T220:00:00Z** — Merged 10 Agent Worktrees: Cherry-picked 9 commits from parallel Opus agents (1 had no commits). First merged shared base (5f22b08) to resolve divergence, then sequentially applied: GAME_DESIGN.md faction standardization, AI FSM helper extraction (try_build/try_train_unit/to_entity_ids/set_rally_points/defense_rally_pos), ToolRegistry LazyLock singleton, combat edge cases (Corroded decay guard, GravitationalPull bounds, resource_deposits budget), build system timing fixes (supply cap deferred, HQ build rejection), harness server dedup (run_behavior/run_query helpers, FactionId::for_player), cc_core utility consolidation (StatusEffects::refresh_or_insert, UnderConstruction::progress_f32, BuildingKind::display_name, BUILDING_SPRITE_SIZE constant), spawn_base_unit extraction (6 duplicated patterns → 1 shared helper), arena/harness dedup (make_headless_world, pub helpers). Resolved 5 merge conflicts (all in TDL.md or harness chain). 592 tests pass, 0 failures. 44 TDL items marked [x].
- **2026-03-01T210:00:00Z** — AI Training Gen 17-23 Combat Micro Breakthrough: FSM now does own production fill (smart_fill counterproductive). Shifted to combat micro approach. Fixed 8+ build errors from user's concurrent faction edits (Croak upgrade names, fmap field, maybe_build_supply args, barracks_kind borrow, player_hint method, push_for_player, duplicate max_tier, Cargo.toml clap placement). Gen 21 BREAKTHROUGH: pure combat_micro.lua (group focus fire weakest enemy + retreat wounded when outnumbered + push to enemy buildings when area clear) = 50% P0 win rate on 10 seeds (0% baseline). Gen 22 (abilities + combat) = 0% (ability commands interfere). Gen 23 (per-unit focus) = 0% (scattered damage). Key insight: concentrated group focus fire is the #1 script behavior; retreat preserves army for attrition; push closes games. Never combine ability and attack scripts for same units.
- **2026-03-01T200:00:00Z** — AI Training Gen 18b + FSM Symmetry Fix: Fixed 4 FSM asymmetries causing P1 70-80% advantage: directional defense_pos toward map center (was hardcoded +3,+3), map-center fallbacks (was GridPos(55,55)), mirrored forward_pos in defend commands, pre-seeded enemy_spawn from SpawnPositions. Added --shared-scripts CLI flag and baselines_clean/ (3 proven scripts). Gen 18b BREAKTHROUGH: activate_abilities.lua (LoafMode/Zoomies/DissonantScreech) + baselines = P0 dominates 8/10 seeds (2 decisive wins + 6 leading timeouts). Key insight: ability activation > extra unit production. smart_fill HURTS when combined with abilities (Gen 18: 10% P0, Gen 18b without it: 80% effective dominance). Updated tracker.json and strategy_guide.md. maybe_build_supply now takes faction-specific supply_kind parameter.
- **2026-03-01T190:00:00Z** — AI Training Gen 10-15 Breakthrough: Continued self-improving AI training pipeline. Fixed build errors from concurrent faction additions. Gen 12 BREAKTHROUGH: smart_fill.lua achieved 53% P0 win rate on 15 seeds (up from 7% baseline). Key insight: FSM leaves production buildings idle; filling idle windows with unit training is the most impactful script behavior.
- **2026-03-01T170:00:00Z** — The Clawed Faction Implementation (Re-applied): Re-implemented The Clawed (Mice/Claudeus Maximus) faction data layer.
- **2026-03-01T160:00:00Z** — Croak Faction + LLAMA Faction Implementation: Fully implemented both factions' data layers. 533 tests pass.
- **2026-03-01T140:00:00Z** — Web Build Phase 6: Provider Selection UI + WASM Deploy. 533 tests pass, WASM+native compile clean, trunk build produces dist/.
- **2026-03-01T120:00:00Z** — Devstral Lua Fine-Tuning Pipeline: Built complete fine-tuning pipeline for Devstral Small 2 as agentic Lua builder. Created: system_prompt.txt (full ctx API ref), 50 gold training examples (JSONL, 5 categories×10), eval_lua.py (syntax+API+runtime scoring), validate_lua_data.py (JSONL format validator), generate_lua_synthetic.py (50→500 augmentation via Claude API), devstral_24b_lua_qlora.yaml (QLoRA config). Updated ARCHITECTURE.md (5-layer system diagram), MISTRAL.md (agentic builder section), BREV_GUIDE.md (single-model focus). 50/50 validation pass, 92% avg eval score. Mistral hackathon ready.
- **2026-03-01T100:00:00Z** — Code Review Fixes: Applied 7 fixes from correctness + refactor reviews: (1) CRITICAL: Fixed Lua field name mismatch in 5 starter scripts (u.is_idle→u.idle, removed u.is_dead — dead units not in snapshot), (2) Fixed clear_in_flight race condition — moved per-player in-flight clearing into poll_agent_responses instead of broken time-based heuristic, (3) VecDeque for AgentChatLog (O(1) vs O(n) pop), (4) Made extract_name_from_source pub and removed _pub wrapper anti-pattern, (5) Fixed resource_deposits Debug→Display format + added kind alias, (6) Updated system prompt field names to match actual Lua bindings, (7) Fixed deprecated set_enabled→disable(). 474 tests pass. 6 items deferred to TDL.
- **2026-03-01T080:00:00Z** — LLM Runner + Construct Mode UI: Implemented end-to-end AI integration: (1) llm_runner.rs — background async task receiving AgentRequests, calling LLM via OpenAiCompatibleClient, multi-turn tool calling (5 rounds max), (2) decision.rs — Bevy timer system (2s interval) for AI-controlled players with in-flight tracking, (3) Construct Mode UI wiring — chat→AgentBridge→LLM→response with system prompt, editable code editor, save/test buttons, loading indicator, (4) agent_bridge.rs — AgentSource routing (GameLoop/ConstructMode/QuickCommand), AgentChatLog, extract_lua_script, (5) 5 starter Lua scripts + get_resources/resource_deposits Lua bindings, (6) agent_chat.rs updated with real response log. 422 tests pass (121 cc_core + 197 cc_sim + 99 cc_agent + 5 cc_harness).
- **2026-03-01T060:00:00Z** — AI FSM Research + Advanced Units Fix: Diagnosed and fixed AI never researching upgrades or training advanced units during Attack/Defend phases. Root causes: (1) ScratchingPost entity set in census during construction (no Researcher component), causing silent command failures; (2) research/advanced-unit code only existed in MidGame phase; (3) AI never accumulated 150 food for SharperClaws due to constant unit training spending. Fixes: added UnderConstruction to building census query, extracted maybe_queue_research + maybe_train_advanced_units helpers called from MidGame/Attack/Defend, smart food-saving (pauses unit training when saving for affordable upgrade). Added 10 unit tests for the helpers. All 3 upgrades now complete reliably. "Research completed" and "Advanced unit trained" added to critical wet test milestones. 211 tests pass.
- **2026-02-28T280:00:00Z** — Phase 4C: Next 10 Abilities + 2 TDL Fixes. Implemented GravitationalChonk, NineLives, ContagiousYawning, PowerNap, TacticalUplink, Hairball, Disoriented, DisgustMortar, ShapedCharge, EcholocationPulse. T1 fix (5 Annoyed→Tilted CC), T2 fix (DreamSiege reset on damage). 4 new deferred commands, 13 tuning constants, PowerNapping status effect, NineLivesTracker + HairballObstacle components. 20 new integration tests. 456 workspace tests pass.
- **2026-02-28T230:00:00Z** — Unit Training Flow: Completed plan steps 4-10. Added BUILDER_PROXIMITY tuning constant, build hotkeys (B+sub-keys), training hotkeys (Q/W/E/R/X), bevy_ui resource HUD, Stop/Move build-cancel refunds, 5 new integration tests. MissionStarted refactored to named struct with mission_id for mission change detection. 431 workspace tests pass. Fixed stale build cache issue with wave_spawner.rs.
- **2026-03-01T013:00:00Z** — Web Build Phase W1+W2: Added `native` feature flag to cc_client, gated cc_agent/cc_voice/bevy_egui behind it. All 11 UI modules cfg-gated. Screenshot and filesystem access gated for wasm32. Trunk.toml + index.html in crates/cc_client/. WASM builds to 152MB debug (1GB raw .wasm). Trunk serve running at localhost:8080. 115+34 tests still pass (pre-existing integration test compile errors unrelated).
- **2026-02-28T220:00:00Z** — Wet Test Session #4: Fixed duplicate test fns in mission.rs, Rust 2024 `ref` patterns, WaveTracker missing from campaign tests, WaveEliminated using entity queries instead of WaveTracker resource. Added ability_effect_system + builder_system to harness chain (correctness review finding). Removed redundant Aura insert in command_system.rs. Updated victory test seeds. 431 workspace + 10 wet tests pass, all games produce victories.
- **2026-02-28T210:00:00Z** — Campaign Gaps Phase 1: Core Systems. Registered wave_spawner module. Added PersistentCampaignState, NextMission enum + ai_tool_tier. WaveEliminated uses WaveTracker resource. 8 new wave integration tests + RON validation. 280 tests pass. 4 Act 1 mission RON files validated.
- (older entries moved to oldpad.md)
## Key Findings
- Player wants hybrid control: direct unit micro + AI agent delegation
- AI agent approach: fine-tuned Mistral generates code/MCP tool calls from natural language instructions
- Inference routing: server-side for competitive, local for practice/SP
- Player commands override AI commands for conflict resolution
- Must use fixed-point math from day 1 for deterministic simulation
- **Devstral 2 (123B)** for server-side competitive ($0.40/$2.00 per 1M tokens), **Devstral Small 2 (24B)** for local SP (Q4_K_M ~14GB, fits RTX 4090)
- Both models are dense transformers with native tool use, 256K context
- MCP `inputSchema` maps directly to Mistral `parameters` (same JSON Schema)
- Fine-tuning supports tool-use training: JSONL with messages + tools arrays, 9-char random tool call IDs, stringified JSON args
- Fine-tune via Mistral API (~$5-10/run) or self-hosted with `mistral-finetune` repo (LoRA rank 64)
- Competitive play cost estimate: ~$0.72 per player per game
- Rust client: single `reqwest` HTTP client works for both API and local (OpenAI-compatible endpoint)
- **Voice Command System**: core game mechanic, not convenience. Push-to-talk → speech-to-text → intent classification → triggers player-authored Lua scripts
- **Construct Mode**: in-game LLM-powered scripting environment where players vibecode Lua agent scripts. Can be used mid-mission.
- **Lua scripting**: chosen for WASM sandbox scripts. LLMs generate Lua well, players can hand-edit, classic game scripting language
- **Voice Buff Mechanic**: command-specific temporary buffs on voice-commanded units (attack→damage, retreat→speed, etc.). Intentionally exploitable — "touch all units" meta is acceptable
- **Speech-to-Text tech**: Web Speech API primary (free, low latency, Chromium), Whisper.js fallback (cross-browser, offline, ~40MB model)
- **Intent classification**: tiered — keyword/regex (<50ms) → fuzzy match (~100ms) → Mistral agent for complex strategy (1-3s)
- **Precedents**: EndWar (70-word vocab, hierarchical commands), Radio General (errors as fiction), Warkestra (hybrid voice+mouse)
- **Picovoice Rhino**: audio-to-intent (no transcription step), free tier 3 users, $6K/yr paid. Worth evaluating for production.
